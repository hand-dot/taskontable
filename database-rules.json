{
    "rules": {
        "users": {
            "$uid": {
                ".read": "auth != null",
                ".write": "$uid === auth.uid"
            }
        },
        "teams": {
            ".read": "auth != null",
            ".write": "auth != null"
        }
    }
}